version: 2.1

jobs:
  build:
    docker:
      - image: nginx
    steps:
      - checkout
      - run: ls -a
      - run: uname -a
      - run: ps -ef
      
workflows:
  build-workflow:
    jobs:
      - build
      